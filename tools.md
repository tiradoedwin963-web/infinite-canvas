# 画布 Agent 图片生成 Tool 手册

本手册只用于 `generate_content.mode = "image"`。文本和视频任务忽略本手册。

## 通用规则

- 用户未指定模型时，默认使用 `gemini-3-pro-image-preview`。
- `model` 只填真实模型 ID，不得添加 `image:` 前缀。
- 画面比例只能从 `1:1`、`4:3`、`3:4`、`16:9`、`9:16` 中选择。用户要求其他比例时，选择同方向最接近的合法值并在回复中说明，例如 `2:3` 改为 `3:4`、`3:2` 改为 `4:3`。
- 最多使用 5 张参考图。参考节点只填入 `reference_node_ids`，不要在提示词中伪造图片内容。
- 提示词按“主体 → 环境 → 构图 → 光线 → 风格 → 细节 → 参考图约束 → 排除项”组织。排除项直接写入 `prompt`，不要创造 `negative_prompt` 字段。
- 如果角色可能涉及受保护 IP 或特定商业版本，`prompt` 不得使用角色、品牌、工作室或版本名称，改为年龄、脸型、发型、体型、服装材质与配色、表情、姿态和时代特征等原创描述。公版角色未指向具体商业版本时不必改写。
- 能通过具体特征描述消除风险时直接改写，不得为原创近似方案再次询问用户。

## Nano Banana Pro

- 模型 ID：`gemini-3-pro-image-preview`
- 适用：默认通用高质量生图、写实人物、编辑感画面和参考图一致性。
- 分辨率：`1K`、`2K`、`4K`。默认 `1K`。
- 参考图：最多 5 张。
- 提示词模板：`[主体与特征]，位于[环境]，[景别和构图]，[光线与色彩]，[写实/编辑/插画风格]，保留[参考图身份或造型约束]，排除[文字、水印、品牌标识、畸变细节]`

## GPT Image 2

- 模型 ID：`gpt-image-2`
- 适用：精确指令跟随、海报版式、画面文字和多元素位置约束。
- 分辨率：`1K`、`2K`、`4K`。默认 `1K`。
- 参考图：最多 5 张。
- 提示词模板：`[画面用途]，主体是[主体]，[构图区域和元素位置]，需要显示的准确文字为“[文字]”，[字体和版式]，[光线、色彩和材质]，排除[多余文字、水印、错乱布局]`

## Nano Banana 2

- 模型 ID：`gemini-3.1-flash-image-preview`
- 适用：快速试稿、多方案迭代、构图探索和中低分辨率预览。
- 分辨率：`0.5K`、`1K`、`2K`、`4K`。默认 `1K`。
- 参考图：最多 5 张。
- 提示词模板：`[核心主体]，[场景]，优先尝试[构图或风格变体]，[光线和主色]，保留[必须一致的参考特征]，排除[文字、水印、多余主体]`

## Seedream 5.0 Pro

- 模型 ID：`doubao-seedream-5-0-pro-260628`
- 适用：中文视觉语境、人物、插画、东方审美和精致场景。
- 分辨率：`1K`、`2K`。默认 `1K`。
- 参考图：最多 5 张。
- 提示词模板：`[主体与中文语境特征]，[环境与时代]，[竖版/横版构图和景别]，[柔和/戏剧光线]，[写实/插画/东方美学]，[衣着、材质和背景细节]，排除[文字、水印、品牌标识、失真人体]`
