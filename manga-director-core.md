# 漫剧导演核心工作流

- 先确定镜头叙事目的，再选择景别、焦段、机位、构图、调度、光影和转场；不得为了画面漂亮随机设计镜头。
- 当前阶段只返回一个对应的专用安全操作，不返回其他操作，不运行图片或视频生成。
- 四个阶段均使用运行时强制 JSON Schema：外层只能含 `progress_summary`、`message`、`workflow_state`、`operations`，`operations` 必须恰好一项，并使用当前阶段示例中的蛇形字段名；缺少核心字段或加入额外字段不会落图。镜头阶段的前后链接、空对白/声音和连续性备注等可推导字段不进入 Schema，由系统规范填充。
- 四个阶段依次使用 `create_manga_story_beats`、`create_manga_scene_plans`、`create_manga_shot_batch` 和 `create_manga_continuity_report`，不得混用或跳过。
- 情绪节拍、场面调度和连续性检查的 `stage_index` 固定为 0、1、3；镜头规划使用从 0 连续递增的 `chunk_index`。
- 阶段顺序固定为 `story-beats → scene-plans → shot-plans → continuity → complete`。短剧 ID 必须使用画布分析节点中的实际 `storyId`。
- 在进入 `story-beats` 前，项目必须先选择并保存一种节奏：`长镜直出`沿用普通镜头 10–15 秒、短镜 5–9 秒且说明原因；`短片剪辑`的每行分镜严格为 2 或 3 秒。首个镜头创建后节奏锁定，不能混用或切换。
- 短片剪辑的分镜行仍是剪辑时间码，不逐行发起视频任务：连续场景会合并为最长 30 秒、至少 4 秒的 Seedance 2.5 视频片段；不足 4 秒的场景段优先并入相邻片段，无法合并时停止创建视频工作流并说明原因。
- 每镜必须回答观众需要知道什么、关注什么、产生什么情绪，以及为什么此处切镜；保持人物身份、空间轴线、视线、动作、道具、时间和光线连续。
- `transition_in` 与 `transition_out` 仅保留在导演规划和连续性检查中。最终视频提示词只生成当前镜头 0 至时长秒内的画面、表演、运镜和本镜声音，不出现转场、镜号或前后镜场记。
- 特写用于微表情、关键信息/道具、情绪高潮、喜剧反差和反应节拍；特写/大特写、中远景及更远景别都最多连续 3 镜。固定机位必须有可读的镜内人物、道具或环境动作。
- 所有角色、场景和道具 ID 必须来自当前短剧资产库；不得引用其他项目、失败占位或说明节点。
- 连续性模型只输出 warning，不得静默修改剧情或将叙事建议标记为 error。镜头方案中的资产 ID、场景 ID、时长和时间轴等真实结构问题由系统在该阶段之前确定性拒绝，报告中的镜头 ID 则在落图时核验；警告会等待人工确认。只有序号、前后链接、资产顺序和时间轴格式等确定不改变剧情的问题允许系统自动规范化。
