# Progress

## 2026-08-05 - Task: 初始化无限画布工程基线

### What was done
- 初始化可运行的 Vinext/React 单页工程，并建立统一开发规范、项目边界和本地开发文档。

### Testing
- `npm ci --ignore-scripts --prefer-offline --no-audit --no-fund`：通过，依赖安装完成。
- `npm run dev`：通过，本地开发服务启动于 `http://localhost:3000/`。
- `npm test`：通过，脚手架构建及服务端渲染测试共 2 项通过。

### Notes
- `AGENTS.md`：写入仓库统一执行规范及 A/B 分支职责边界。
- `docs/canvas.md`：记录画布范围、分支职责、本地运行和验证方式。
- `progress.md`：建立只追加的任务进度日志。
- 其余工程文件：由 Sites Vinext 标准脚手架初始化。
- 回滚方式：将仓库回退到本任务的父提交；由于本任务是首个提交，也可删除该提交后恢复为空仓库。

## 2026-08-05 - Task: 实现基础空白无限画布

### What was done
- 在 `codex/canvas-a` 实现全屏浅色点阵画布，支持鼠标、触控笔和单指拖动平移，以及围绕指针位置的 25%–400% 缩放。
- 移除脚手架预览、鉴权、数据库和示例占位，仅保留当前画布所需的运行表面与依赖。
- 将视口计算拆为可测试的纯函数，并更新服务端渲染与视口行为测试。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，服务端渲染和视口行为共 5 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- `npm run dev`：通过，最终画布在 `http://localhost:3000/` 返回 HTTP 200。

### Notes
- `README.md`：替换脚手架说明，记录画布本地运行与验证入口。
- `app/page.tsx`：实现空白画布的指针拖动和滚轮缩放输入。
- `app/canvas/viewport.ts`：新增平移、缩放边界及定点缩放计算。
- `app/globals.css`：实现全屏暖白背景、低对比点阵和抓取光标。
- `app/layout.tsx`：设置中文页面语言、画布标题和描述。
- `tests/rendered-html.test.mjs`：改为验证画布页面及脚手架清理结果。
- `tests/viewport.test.mjs`：新增平移、缩放范围和锚点稳定性测试。
- `package.json`：更新项目名称、测试入口并移除未使用依赖与数据库脚本。
- `package-lock.json`：同步当前最小依赖集合。
- `app/_sites-preview/SkeletonPreview.tsx`：删除脚手架加载界面。
- `app/_sites-preview/preview.css`：删除脚手架加载样式。
- `app/chatgpt-auth.ts`：删除未使用的鉴权占位实现。
- `db/index.ts`：删除未使用的数据库入口。
- `db/schema.ts`：删除未使用的数据库结构占位。
- `drizzle.config.ts`：删除未使用的数据库生成配置。
- `drizzle/meta/_journal.json`：删除未使用的迁移记录占位。
- `examples/d1/app/api/notes/route.ts`：删除未使用的数据库接口示例。
- `examples/d1/db/schema.ts`：删除未使用的数据库示例结构。
- `public/favicon.svg`：删除未使用的脚手架图标。
- `public/file.svg`：删除未使用的脚手架图标。
- `public/globe.svg`：删除未使用的脚手架图标。
- `public/window.svg`：删除未使用的脚手架图标。
- `progress.md`：追加本轮实现与验证记录。
- 回滚方式：执行 `git revert $(git log --grep='feat: add empty infinite canvas' --format=%H -n 1)`。

## 2026-08-05 - Task: 同步并验证画布 Agent 工作区

### What was done
- 创建 `codex/canvas-a` 与 `codex/canvas-b` 两个相邻 worktree，并将 B 快进到已验证的基础画布提交。
- 确认 B 当前只继承基础画布与统一开发规范，不包含 Agent UI、接口或占位功能。

### Testing
- `git merge --ff-only codex/canvas-a`：通过，B 无冲突同步基础画布提交。
- 在 B 执行 `npm ci --ignore-scripts --prefer-offline --no-audit --no-fund`：通过，安装 462 个依赖包。
- 在 B 执行 `npm run lint`：通过，无 ESLint 错误。
- 在 B 执行 `npm test`：通过，Vinext 构建成功，5 项测试全部通过。

### Notes
- `progress.md`：追加双 worktree 同步及 B 分支独立验证证据。
- B 同步前回滚点：`fd12103b128b419059d05d9ae4031192895b61c9`。

## 2026-08-06 - Task: 实现无限画布基础节点图

### What was done
- 在不修改基础视口协议的前提下，实现双击选择文本、生图或生视频节点，以及节点拖动、编辑和删除。
- 实现任意节点类型之间的定向拖线、四向自动吸附、多来源提示词展示与单条断开，并阻止自连接和重复连接。
- 接入可展开的底部提示词编辑器，只保存文字而不发起生图、生视频或其他网络请求。
- 使用版本化浏览器本地数据保存节点、位置、文字和连线，刷新后可恢复节点图。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，节点图、服务端渲染和视口行为共 10 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- 本地浏览器验收：通过三类节点创建与文本编辑、节点拖动、跨类型连线、来源提示词展示、删除级联清线和刷新恢复；最终验证 3 个节点与 2 条连线恢复正常，控制台无错误或警告。

### Notes
- `README.md`：更新项目定位为可编辑和连接的提示词节点画布。
- `app/canvas/graph.ts`：新增节点图类型、增删改、持久化校验与连线几何计算。
- `app/page.tsx`：接入节点创建、拖动、选中、连线、删除、编辑和本地恢复交互。
- `components/ui/ai-chat-input.tsx`：新增受控底部提示词编辑器与直接来源管理界面。
- `app/globals.css`：新增节点卡片、类型选择器、连线和底部编辑器的浅色视觉与选中层级。
- `app/layout.tsx`：更新页面标题和描述以反映节点图能力。
- `package.json`：增加动画与图标依赖，并将节点图测试加入统一测试入口。
- `package-lock.json`：锁定 `motion` 与 `lucide-react` 及其相关依赖。
- `tests/graph.test.mjs`：新增节点、连线、级联删除、持久化和四向吸附测试。
- `tests/rendered-html.test.mjs`：更新服务端渲染断言为节点画布页面。
- `docs/canvas.md`：记录节点交互、连线规则、本地保存和当前不生成内容的边界。
- `progress.md`：追加本轮实现、验证、文件清单与回滚点。
- 回滚点：`fe1d0d4e8e6e0c783b1f0602f1bdcfa15a49b779`；本轮改动形成独立提交后，可执行 `git revert <本轮提交哈希>` 回滚。

## 2026-08-06 - Task: 回退无限画布基础节点图

### What was done
- 按用户确认停止本地开发服务，移除本轮新增的节点图、连线、底部编辑器和本地持久化能力。
- 移除本轮新增的动画与图标依赖，恢复为只支持平移、缩放的基础空白无限画布。
- 保留原有基础视口协议和历史进度记录，当前代码状态与 `fe1d0d4` 基础画布一致。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，基础画布服务端渲染与视口行为共 5 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- `git status --short`：除本轮按规范追加的 `progress.md` 外，节点图相关工程文件均已恢复或移除。

### Notes
- `README.md`：恢复基础空白无限画布的项目说明。
- `app/globals.css`：移除节点、连线、选择器和底部编辑器样式。
- `app/layout.tsx`：恢复基础无限画布的页面标题和描述。
- `app/page.tsx`：恢复仅包含平移、缩放输入的空白画布页面。
- `app/canvas/graph.ts`：移除本轮新增的节点图数据与几何计算。
- `components/ui/ai-chat-input.tsx`：移除本轮新增的底部提示词编辑器。
- `docs/canvas.md`：恢复基础画布范围、分支职责与交互说明。
- `package.json`：移除节点图测试入口与新增依赖。
- `package-lock.json`：恢复基础画布依赖锁定状态。
- `tests/graph.test.mjs`：移除本轮新增的节点图测试。
- `tests/rendered-html.test.mjs`：恢复空白无限画布的服务端渲染断言。
- `progress.md`：保留历史并追加本轮回退内容与验证证据。
- 当前回滚点：`fe1d0d4e8e6e0c783b1f0602f1bdcfa15a49b779`；节点图功能需要恢复时，应以前一轮记录的实现范围重新施工。

## 2026-08-06 - Task: 接入画布底部多模式 AI 输入组件

### What was done
- 在基础空白画布底部接入可展开的 AI 输入组件，保持组件与画布平移、缩放输入相互隔离。
- 提供文本、图片、视频三种模式，并按模式限定 `GPT-5.6 Sol` / `GPT-5.5`、`Nano Banana Pro` / `Image 2` 与 `Seedance` 模型列表。
- 实现模式占位文字动画、自动选择该模式首个模型和输入内容保留；发送按钮保持禁用，不调用模型或创建节点。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，输入组件服务端渲染与原有视口行为共 5 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- 本地浏览器验收：通过组件展开与收起、文本输入保留、三种模式与五个模型的过滤联动、占位文字切换和发送按钮禁用验证；组件内拖动与滚动前后画布视口参数保持不变，控制台无错误或警告。

### Notes
- `components/ui/ai-chat-input.tsx`：新增自包含的多模式 AI 输入组件、模型映射和禁用提交状态。
- `app/page.tsx`：将底部输入组件接入现有无限画布，不修改基础视口协议。
- `app/layout.tsx`：更新页面描述以反映多模式输入组件。
- `package.json`：增加 `motion` 与 `lucide-react` 运行依赖。
- `package-lock.json`：锁定新增动画与图标依赖。
- `tests/rendered-html.test.mjs`：更新页面渲染、默认模式、默认模型和未接入生成行为的断言。
- `docs/canvas.md`：记录模式、模型联动、刷新默认值和禁用提交边界。
- `progress.md`：保留现有历史并追加本轮实现、验证和文件清单。
- 回滚点：`fe1d0d4e8e6e0c783b1f0602f1bdcfa15a49b779`；本轮改动形成独立提交后，可执行 `git revert <本轮提交哈希>` 回滚输入组件。

## 2026-08-06 - Task: 按参考代码调整 AI 输入组件 UI

### What was done
- 将画布底部输入组件调整为参考代码的 68px 收起、128px 展开尺寸、32px 圆角与弹簧阴影视觉。
- 将占位文字改为逐字错峰、模糊和位移动画，并将控制栏调整为参考代码的上浮显示节奏。
- 保留文本、图片、视频模式与对应模型过滤，不增加附件、语音、Think 或 Deep Search 功能，发送按钮仍为禁用状态。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，输入组件与基础画布共 5 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- 本地浏览器验收：确认收起高度 68px、展开目标高度 128px、逐字动画正常，图片模式仅展示 `Nano Banana Pro` 与 `Image 2`；组件内滚动前后画布视口参数不变，控制台无错误或警告。

### Notes
- `components/ui/ai-chat-input.tsx`：对齐参考组件的尺寸、阴影、圆角、发送图标、逐字占位动画和展开控制栏视觉。
- `tests/rendered-html.test.mjs`：增加 68/128px 尺寸、错峰字符动画与逐字拆分的静态断言。
- `progress.md`：追加本轮 UI 调整、验证证据和文件清单。
- 回滚点：本轮 UI 调整前的多模式输入组件状态；形成独立提交后可执行 `git revert <本轮提交哈希>` 回滚。

## 2026-08-06 - Task: 接入 LingkeAI 多模型生成与画布节点

### What was done
- 将底部输入组件接入同源服务端代理，文本、图片、视频三种模式各提供 5 个白名单模型，并统一处理三种文本协议与异步媒体任务。
- 增加参考图上传、缩略图删除、比例和视频时长参数，以及附件数量、类型和体积校验；提交时自动创建输入、输出节点与连线。
- 实现生成节点拖动、单独删除、媒体进度与手动续查，多结果拆分为独立节点；节点、任务和素材分别通过版本化 `localStorage` 与 IndexedDB 恢复。
- 将 LingkeAI 地址和凭据写入被 Git 忽略的服务端本地环境文件，客户端、文档、日志与受版本控制文件均不包含密钥。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，模型映射、协议适配、参数校验、节点图、持久化解析、服务端渲染和视口行为共 18 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- API 无费用冒烟测试：未知模型与缺少任务参数均返回统一的 400 响应，未向上游发起生成请求。
- 本地浏览器验收：确认三种模式各显示 5 个模型、图片上传与删除、比例与时长选项、组件事件隔离均正常，控制台无错误或警告。
- 未执行真实付费生成：当前余额为 0；上游协议和错误归一化使用模拟响应完成自动化验证，充值后仍需补一次真实端到端验收。

### Notes
- `.env.local`：保存本机服务端地址和凭据，已由 Git 忽略且未记录具体值。
- `README.md`：更新项目定位、本地启动方式和私有环境安全边界。
- `app/ai/models.ts`：集中定义 15 个模型、协议、默认值和可用生成参数。
- `app/ai/types.ts`：定义生成请求、同步结果和异步任务状态接口。
- `app/ai/provider.ts`：实现请求校验、三种文本协议、媒体任务和安全错误归一化。
- `app/api/ai/generate/route.ts`：新增只读取服务端环境变量的生成代理入口。
- `app/api/ai/status/route.ts`：新增异步媒体任务状态查询代理入口。
- `app/canvas/assets.ts`：使用 IndexedDB 保存和恢复本地参考图 Blob。
- `app/canvas/graph.ts`：实现生成节点、自动连线、多结果拆分、任务状态和版本化图数据解析。
- `app/page.tsx`：接入节点画布、生成提交、任务轮询、拖动、删除和刷新恢复。
- `components/ui/ai-chat-input.tsx`：扩展多模式输入、15 个模型、附件、比例、时长和提交校验界面。
- `app/globals.css`：增加节点、连线、任务状态和生成结果的画布视觉。
- `app/layout.tsx`：更新页面标题和说明为 LingkeAI 无限画布。
- `docs/canvas.md`：记录模型清单、生成流程、持久化、轮询和安全使用说明。
- `package.json`：更新统一测试入口并保留输入组件所需依赖。
- `package-lock.json`：同步锁定运行依赖。
- `tests/models.test.mjs`：覆盖 15 个模型、默认值和能力边界。
- `tests/provider.test.mjs`：覆盖协议适配、媒体任务、校验和错误归一化。
- `tests/graph.test.mjs`：覆盖节点拆分、自动连线、删除、布局、持久化和连线几何。
- `tests/rendered-html.test.mjs`：更新 LingkeAI 画布的服务端渲染断言。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚点：`fe1d0d4e8e6e0c783b1f0602f1bdcfa15a49b779`；形成独立提交后可执行 `git revert <本轮提交哈希>`，并删除本机 `.env.local`、清理浏览器站点数据以移除本地任务和素材。

## 2026-08-06 - Task: 修复图片生成与固定输入组件布局

### What was done
- 按 LingkeAI 实际模型协议修复 5 个图片模型的比例、分辨率、质量和参考图参数映射，并将 Qwen Image 参考图上限调整为 3 张。
- 扩展异步媒体任务的嵌套状态、平台状态枚举和多结果图片解析；参数错误返回经过清理的具体原因，继续隐藏鉴权和请求素材等敏感信息。
- 将底部输入组件固定为三列布局和状态化固定高度，使三种模式的输入与发送位置一致，并保持桌面固定宽度、窄屏安全适配和画布缩放隔离。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，图片参数映射、任务归一化、安全错误、节点图、服务端渲染和视口行为共 20 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- 真实模型验收：Nano Banana Pro、GPT Image 2、Nano Banana 2、Seedream 5.0 Pro、Qwen Image 均以 1:1、1K、无参考图条件单次提交成功，任务完成并返回图片地址；没有重复提交已受理任务。
- 本地浏览器验收：1280px 视口下组件宽 768px，800px 视口下宽 752px并保留两侧 24px；三种模式输入起点与发送按钮坐标一致，画布缩放至约 1.52 倍后组件尺寸和底部 24px 间距不变，控制台无错误或警告。

### Notes
- `app/ai/models.ts`：增加图片模型请求映射并修正 Qwen Image 参考图上限。
- `app/ai/provider.ts`：按模型生成平台参数，扩展媒体状态、结果和安全错误归一化。
- `components/ui/ai-chat-input.tsx`：固定组件宽高、底部间距与三列对齐布局。
- `tests/models.test.mjs`：增加图片模型映射与参考图能力断言。
- `tests/provider.test.mjs`：增加 5 个图片模型参数、嵌套状态、多结果和错误清理测试。
- `tests/rendered-html.test.mjs`：增加固定宽度、三列布局与文本对齐的静态断言。
- `docs/canvas.md`：补充图片参数、输入框尺寸、错误提示与真实验收记录。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“接入 LingkeAI 多模型生成与画布节点”记录对应的工作树状态。

## 2026-08-06 - Task: 恢复上传按钮并修复展开布局

### What was done
- 恢复文本、图片和视频三种模式下始终可见的图片上传入口，并让五个文本模型真实接收最多 5 张图片附件。
- 分别按 OpenAI、Anthropic 和 Gemini 协议构造多模态文本请求，无附件时保持原有纯文本请求结构。
- 将无附件展开高度压缩为 128px，保留聚焦后的动态占位，并让控制栏、缩略图和错误提示与正文列左右对齐。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，多模态文本协议、附件限制、图片模型、节点图、服务端渲染和视口行为共 21 项测试全部通过。
- `git diff --check`：通过，无空白符错误。
- 真实多模态验收：使用临时生成的 64×64 纯红 PNG，通过 GPT-5.6 Sol 单次提交成功，模型返回“红色”；画布生成文本输入、图片输入和文本输出共 3 个节点及 2 条自动连线。
- 本地浏览器验收：735×292、800px 和 1280px 视口下上传、输入和发送均完整可见；展开高度为 128px，带缩略图时为 192px，占位保持显示，控制栏与正文列对齐；三种模式横坐标一致，控制台无错误或警告。

### Notes
- `app/ai/models.ts`：为五个文本模型开放最多 5 张图片附件。
- `app/ai/provider.ts`：增加三种文本协议的多模态图片消息构造并保留纯文本兼容行为。
- `components/ui/ai-chat-input.tsx`：恢复全模式上传按钮，压缩展开高度并修复占位和内容对齐。
- `tests/models.test.mjs`：增加文本模型附件能力断言。
- `tests/provider.test.mjs`：增加三种文本协议图片结构、Base64 提取和附件上限测试。
- `tests/rendered-html.test.mjs`：增加上传按钮、紧凑高度、占位和控制栏对齐断言。
- `docs/canvas.md`：补充文本图片输入、协议映射与最新输入组件尺寸说明。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“修复图片生成与固定输入组件布局”记录对应的工作树状态。

## 2026-08-06 - Task: 固化输入框向上展开与占位文字联动

### What was done
- 将占位文字轮播直接绑定到统一展开状态，确保聚焦、已输入或已添加附件时不再显示或后台切换占位文字。
- 固化底部对齐展开行为：收起为 68px，无附件展开为 128px，输入和控制区整体贴底，额外空间位于顶部。
- 更正画布文档中与当前交互相反的占位文字说明，并增加展开条件回归断言。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 21 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 本地浏览器验收：1280px 视口下收起高度 68px、展开高度 128px、距底部 24px；展开后顶部留白约 15px，输入行与控制栏整体贴底。展开时占位文字消失，输入内容后保持展开，清空并点击外部后收起且占位文字恢复。
- 窄屏规则通过 `w-[768px] max-w-full` 与容器 `px-6` 的静态回归断言；当前浏览器验收环境不支持切换视口，本轮未将 735px/800px 记为实际窗口验收。

### Notes
- `components/ui/ai-chat-input.tsx`：让占位轮播与统一展开状态保持一致。
- `tests/rendered-html.test.mjs`：增加聚焦、输入和附件的展开条件及占位轮播停止断言。
- `docs/canvas.md`：记录向上展开、内容贴底和占位文字联动规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“恢复上传按钮并修复展开布局”记录对应的工作树状态。

## 2026-08-06 - Task: 对齐展开态底部操作栏

### What was done
- 保留收起态“上传 + 正文 + 发送”单行布局，展开后将正文区独立放在上方。
- 将上传和发送固定在底部两侧，模式、模型、比例和时长位于中间可滚动区域，所有操作控件垂直中心对齐。
- 将缩略图和错误提示移到正文与底部操作栏之间，保持现有 128px 基础展开高度及附件/错误增量规则。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 21 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 本地浏览器验收：收起态高度为 68px，上传、正文和发送中心线误差为 0px；展开态高度为 128px，文本模式的上传/模式/模型/发送、图片模式增加比例后以及视频模式增加比例和时长后，所有操作控件中心线误差均为 0px。

### Notes
- `components/ui/ai-chat-input.tsx`：重排展开态正文、缩略图/错误与底部操作栏，并精确对齐两侧按钮和中间选择器。
- `tests/rendered-html.test.mjs`：增加上传/发送固定位置、60/68px 两层布局、中间滚动区及缩略图顺序断言。
- `docs/canvas.md`：记录展开态底部操作栏、窄屏滚动和内容层级。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“固化输入框向上展开与占位文字联动”记录对应的工作树状态。

## 2026-08-06 - Task: 修复展开瞬间占位文字与上传按钮穿模

### What was done
- 确认穿模由展开时正文区水平内边距从 64px 瞬间变为 24px 引起，占位文字退场动画因此左移并进入上传按钮区域。
- 收起和展开状态统一使用 64px 水平内边距，展开过程只切换正文区高度，消除占位文字的横向跳动。
- 保留底部操作栏、占位退场动画、上传和生成行为不变。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 21 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 本地浏览器验收：修复前展开瞬间占位文字左边缘从 333px 移到 293px，与右边缘为 313px 的上传按钮发生重叠；修复后文字左边缘稳定为 333px，与上传按钮保持 20px 间距。展开后上传、模式、模型和发送中心线误差仍为 0px，高度仍为 128px。

### Notes
- `components/ui/ai-chat-input.tsx`：固定正文区展开前后的水平内边距。
- `tests/rendered-html.test.mjs`：增加展开态不得切换为 24px 水平内边距的回归断言。
- `docs/canvas.md`：记录展开动画的水平位置稳定规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“对齐展开态底部操作栏”记录对应的工作树状态。

## 2026-08-06 - Task: 优化画布触控板缩放与图片展示

### What was done
- 将画布缩放改为原生非被动滚轮监听，支持鼠标滚轮、触控板双指滚动与捏合，保持以指针位置为锚点和 25%–400% 缩放边界。
- 缩放只处理画布空白区域直接触发的手势，节点、图片和底部输入组件上的滚轮不再改变画布缩放。
- 已有图片内容的图片节点改为透明无边框展示，使用 `contain` 按原始比例展示完整图片，移除标题、模型和状态页脚；删除按钮改为悬停/聚焦时显示的覆盖控件。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 22 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 视口单元测试覆盖滚轮放大/缩小、捏合灵敏度及大幅度输入限制；页面回归断言覆盖非被动监听、画布直接目标过滤和移除 React `onWheel`。
- 本地浏览器图片验收：已有图片节点的边框为 0、背景透明、无阴影、`object-fit` 为 `contain`，且不再渲染标题和页脚。当前浏览器控制环境不支持合成原生触控板 `WheelEvent`，本轮未将触控板记为真实硬件手势验收。

### Notes
- `app/page.tsx`：接入画布限定的原生缩放监听，并为已有图片的节点切换无框渲染结构。
- `app/canvas/viewport.ts`：增加带幅度限制的滚轮/捏合缩放因子计算。
- `app/globals.css`：增加透明无框图片、完整比例展示和悬停删除按钮样式。
- `tests/viewport.test.mjs`：增加鼠标滚轮与触控板捏合缩放因子测试。
- `tests/rendered-html.test.mjs`：增加画布限定监听与无框完整图片的结构/样式断言。
- `docs/canvas.md`：更新触控板缩放范围和图片节点展示规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“修复展开瞬间占位文字与上传按钮穿模”记录对应的工作树状态。

## 2026-08-06 - Task: 实现节点加号创建、手动连线与真实生成

### What was done
- 为所有节点增加左侧上游和右侧下游加号，支持单击选择文本、图片或视频类型，按方向创建空节点、自动连接并避让已有节点。
- 增加从加号拖到任意节点的手动连线，包含临时曲线、目标高亮、边缘吸附、取消交互，并拒绝自连接和同向重复连接。
- 手动新节点自动选中并锁定底部输入框类型；提交时按连线顺序读取直接上游文本与图片，并将同步或异步结果直接写回原节点。
- 补齐空文本、图片和视频节点的可见空态，并让手动媒体节点参与现有轮询与多结果拆分。
- 浏览器验收时发现嵌套类型菜单会覆盖节点，已将菜单改为画布世界中的独立同级浮层，确保位置正确且不裁切。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 28 项测试通过。
- 节点图单元测试覆盖左右创建方向、垂直碰撞避让、删除级联清线、自连接与重复连接拒绝、多来源、环形关系、拖线路径、直接上游顺序及图片/视频上下文规则。
- 本地浏览器验收：已有三个节点的左右加号全部可见；右侧菜单正确展示三类节点；临时创建视频节点后自动避让、自动连线、显示空态，且底部模式选择器锁定。验收后已删除临时节点，节点数量恢复到验收前。
- 本轮没有提交真实模型请求，没有产生模型费用；真实调用复用已通过的服务端路由，本轮使用模拟与构建回归验证。
- `git diff --check`：通过，无空白符错误。

### Notes
- `app/canvas/graph.ts`：增加安全连线、相邻节点创建、直接上游组合、拖线路径及手动节点持久化字段。
- `app/page.tsx`：接入加号、类型菜单、手动拖线、选中态、空状态、直接上游生成与媒体任务轮询。
- `components/ui/ai-chat-input.tsx`：增加可选锁定节点模式，保留对应类型的模型选择。
- `app/globals.css`：增加节点加号、类型菜单、预览线、目标/选中高亮和空状态样式。
- `tests/graph.test.mjs`：增加节点创建、连线约束、直接上游和持久化兼容测试。
- `tests/rendered-html.test.mjs`：增加加号、菜单浮层、拖线、媒体轮询和输入模式锁定的页面回归断言。
- `docs/canvas.md`：记录节点创建、手动连线、直接上游执行与当前能力边界。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“优化画布触控板缩放与图片展示”记录对应的工作树状态。

## 2026-08-06 - Task: 修复全类型节点加号并改为悬停显示

### What was done
- 确认文本和视频节点的左右加号已渲染但被普通卡片的外侧绘制边界裁切，而无框图片节点不受影响。
- 将每个节点的左右加号改为与卡片共享世界坐标的独立浮层，文本、图片和视频节点使用同一渲染路径，不再依赖卡片溢出绘制。
- 加号默认透明隐藏，鼠标进入卡片、点击卡片或进入加号扩展命中区时同时显示两侧，移开后立即隐藏。
- 正在拖线的来源节点强制保持加号可见，单个按钮获得键盘焦点时也会显示；原有菜单、拖线和节点选中协议保持不变。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 28 项测试通过。
- 页面回归断言覆盖独立加号浮层、默认隐藏、14px 扩展命中区、卡片触发显示、拖线强制显示和键盘焦点显示。
- 本地浏览器验收：默认状态下三个已有节点的加号全部隐藏；点击文本输入、文本输出、图片和临时视频节点后均显示左右两个加号；点击空白后全部隐藏。
- 隐藏状态下点击加号附近命中区可正常打开节点类型菜单；临时视频节点已删除，节点数量恢复到验收前，未发起任何生成请求。
- `git diff --check`：通过，无空白符错误。

### Notes
- `app/page.tsx`：将加号抽离为画布同级浮层，增加节点进入、移出和拖线显示状态。
- `app/globals.css`：移除普通卡片滤镜合成层，增加加号浮层、透明隐藏、扩展命中区和显示动画。
- `tests/rendered-html.test.mjs`：增加三类节点共用加号浮层及显隐规则的静态回归断言。
- `docs/canvas.md`：记录加号默认隐藏、附近触发、移开隐藏和拖线保持显示规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“实现节点加号创建、手动连线与真实生成”记录对应的工作树状态。

## 2026-08-06 - Task: AI 输入框紧凑化与图片置顶布局

### What was done
- 将底部输入组件整体缩小到原尺寸约 80%，桌面宽度调整为 614px，收起/展开基础高度调整为 54px/102px，按钮、字体、图标、圆角和间距同步紧凑化。
- 将展开内容固定为“缩略图、文字输入、错误提示、底部操作栏”的顺序，缩略图缩小为 44px 并在顶部从左向右排列。
- 统一缩略图、文字、错误提示和选择器的左边界；收起态保留动态占位动画，展开空输入时显示稳定的静态文字提示。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 28 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 页面回归断言覆盖 614px 宽度、54px/102px 基础高度、51px 缩略图增量、19px 错误增量、44px 缩略图以及图片、文字、操作栏的源码顺序。
- 本地浏览器验收：收起态底部居中且尺寸稳定；展开态显示静态输入提示；现有已选参考图在顶部以紧凑缩略图展示，文字位于其下，底部操作栏贴底并左对齐。文本、图片、视频模式切换均未出现水平跳动，未提交生成请求。

### Notes
- `components/ui/ai-chat-input.tsx`：缩小组件视觉尺寸，重排缩略图、文字和底部操作栏，并增加展开态静态输入提示。
- `tests/rendered-html.test.mjs`：更新紧凑尺寸、统一左边界和图片置顶顺序的静态回归断言。
- `docs/canvas.md`：记录新的宽高、缩略图尺寸、展开顺序和占位文字规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“修复全类型节点加号并改为悬停显示”记录对应的工作树状态。

## 2026-08-06 - Task: 修复输入占位消失与展开态左对齐

### What was done
- 删除展开态的静态占位文字，动态占位仅在收起且输入为空时渲染，展开退场后不再自动回填。
- 将展开态的缩略图、文字输入和错误提示统一为 20px 左边界，文字可显示在左下上传按钮正上方。
- 收起态继续为两侧按钮保留 31px 内部空间，并将占位退场位置固定在原水平坐标，避免展开时左移穿模。

### Testing
- `npm run lint`：通过，无 ESLint 错误。
- `npm test`：通过，Vinext 构建成功，全部 28 项测试通过。
- `git diff --check`：通过，无穽白符错误。
- 页面回归断言覆盖展开态不渲染占位、收起态占位条件、缩略图/文字/错误 20px 左边界、收起态 31px 按钮避让和底部选择区位置不变。
- 本地浏览器验收：展开 700ms 后占位文字从 DOM 中消失且不再回填；输入“左对齐测试”后文字位于组件左边约 20px，正对上传按钮上方；空输入点击画布收起后动态占位恢复。验收中未提交生成请求。

### Notes
- `components/ui/ai-chat-input.tsx`：移除展开占位回填，调整展开内容与收起输入的左边界。
- `tests/rendered-html.test.mjs`：更新占位条件、20px 左对齐和 31px 按钮避让的静态回归断言。
- `docs/canvas.md`：记录占位只在收起态轮播及展开内容的左对齐规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“AI 输入框紧凑化与图片置顶布局”记录对应的工作树状态。

## 2026-08-06 - Task: 增加节点双击编辑与媒体放大查看

### What was done
- 为文本、图片和视频节点增加双击详情入口，统一使用固定在视口中央的遮罩弹层，不参与画布平移缩放。
- 文本节点弹层支持多行编辑、明确保存/取消和 `Cmd/Ctrl + Enter` 保存；保存只更新节点文字，不重新调用模型。
- 图片弹层复用本地素材或生成结果地址并按 `contain` 完整展示；视频弹层使用不自动播放的原生控制栏播放器。空媒体节点不打开弹层。
- 弹层内的指针、滚轮和双击事件与画布隔离，支持关闭按钮、遮罩和 `Escape` 关闭，原节点拖动、删除、加号和连线协议保持不变。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 29 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 节点图单元测试覆盖只更新目标文本、保留其他字段/节点/连线，以及序列化后重新解析仍保留编辑内容。
- 页面回归断言覆盖双击阻止传播、文本保存/快捷键、弹层层级、图片 `contain` 和视频控制栏且不自动播放。
- 本地浏览器验收：文本输入节点双击后正确自动聚焦，取消未写回，保存后卡片立即更新；验收内容随后已恢复为原文。现有本地图片双击后以完整比例居中放大。当前验收画布没有视频节点，视频本轮仅完成结构、构建与静态回归验证，未生成或提交视频请求。

### Notes
- `app/page.tsx`：增加节点双击入口、详情弹层状态、文本保存及图片/视频查看组件。
- `app/globals.css`：增加固定遮罩、文本编辑器、媒体完整预览和操作按钮样式。
- `tests/graph.test.mjs`：增加文本节点编辑及持久化兼容测试。
- `tests/rendered-html.test.mjs`：增加双击弹层、编辑、媒体展示和事件隔离回归断言。
- `docs/canvas.md`：记录节点文本编辑、图片放大、视频查看和关闭规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“修复输入占位消失与展开态左对齐”记录对应的工作树状态。

## 2026-08-06 - Task: 修复本地上传图片放大预览破图

### What was done
- 统一管理本地上传图片的临时 `blob:` 地址，在组件清理、重新挂载、素材替换和节点删除时同步更新加载标记并释放旧地址。
- 本地图片地址失效后由卡片或放大弹层触发一次并发受控的 IndexedDB 恢复，恢复成功后两处共同使用新地址；新上传图片不会被恢复流程重复创建地址。
- IndexedDB 原始素材缺失或新地址再次加载失败时停止自动重试，并以“图片素材已失效，请重新上传”替代破图和黑色空框。
- 生成图片的远程结果地址、节点结构、IndexedDB 结构和生成协议保持不变。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 29 项测试通过。
- 页面回归断言覆盖新上传素材标记、清理时重置加载状态、失效地址替换与释放、卡片/弹层共用错误恢复及缺失素材停止重试。
- 本地浏览器验收：刷新后图片卡片与放大弹层均从 IndexedDB 恢复为可见的 `blob:` 图片；保持弹层打开并触发两次开发热更新后，临时地址确实被替换，卡片和弹层仍可见且未出现素材错误提示。验收未上传用户截图，也未提交生成请求。
- `git diff --check`：通过，无空白符错误。

### Notes
- `app/page.tsx`：增加本地素材地址恢复、并发与重试控制、加载失败提示及卡片/弹层共用回调。
- `app/globals.css`：增加图片卡片和详情弹层的素材失效提示样式。
- `tests/rendered-html.test.mjs`：增加本地素材 URL 生命周期和错误恢复的静态回归断言。
- `docs/canvas.md`：记录本地预览地址失效后的 IndexedDB 恢复及原始素材缺失提示。
- `progress.md`：追加本轮根因、实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“增加节点双击编辑与媒体放大查看”记录对应的工作树状态。

## 2026-08-06 - Task: 增加图片与视频分辨率选择

### What was done
- 在底部聊天框为图片和视频模式增加按模型过滤的分辨率选择器，图片默认 1K、视频默认 720p，切换模式或模型后自动恢复对应默认值。
- 图片模型保留 Nano Banana Pro、GPT Image 2、Nano Banana 2、Seedream 5.0 Pro；视频模型保留 Seedance 3.5 Pro、Vidu Q3，移除没有多档独立分辨率的媒体模型。
- 服务端将所选分辨率按模型映射到 `imageSize`、`size` 或 `resolution`；GPT Image 2 按比例与 1K/2K/4K 组合映射为平台像素尺寸。
- 独立生成和手动节点执行均传递分辨率，服务端拒绝缺失、越权分辨率和已移除模型；节点与持久化版本保持不变。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 32 项测试通过。
- 模型与协议测试覆盖 4 个图片模型、2 个视频模型、默认值、移除模型、非法/缺失分辨率、GPT Image 2 全部 15 组比例与清晰度像素映射，以及 Seedance/Vidu 请求字段。
- 本地浏览器验收：图片模式仅显示 4 个保留模型，各模型分辨率选项正确，Nano Banana 2 与 Seedream 切换后默认 1K；视频仅显示 Seedance 和 Vidu，均默认 720p；文本模式不显示分辨率，紧凑底栏未裁切控件。验收未提交生成请求。
- `git diff --check`：通过，无空白符错误。

### Notes
- `app/ai/models.ts`：配置保留模型的分辨率、默认值和上游字段映射，并移除固定或无独立分辨率模型。
- `app/ai/types.ts`：为生成请求增加可选分辨率字段。
- `app/ai/provider.ts`：增加服务端分辨率校验、图片像素映射及视频分辨率参数。
- `components/ui/ai-chat-input.tsx`：增加分辨率状态、默认重置、提交校验和底部选择器。
- `app/page.tsx`：在独立生成和手动节点执行请求中传递分辨率。
- `tests/models.test.mjs`：覆盖媒体模型清单、分辨率能力、默认值和移除模型。
- `tests/provider.test.mjs`：覆盖分辨率校验及图片、视频上游参数映射。
- `tests/rendered-html.test.mjs`：覆盖聊天框选择器和两条提交流程的静态回归。
- `docs/canvas.md`：更新当前媒体模型、分辨率选项、默认值和协议映射说明。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“修复本地上传图片放大预览破图”记录对应的工作树状态。

## 2026-08-06 - Task: 修复节点自适应连线并增加四角缩放

### What was done
- 为节点增加可选宽高并统一节点尺寸读取，正式连线、拖线预览、左右加号、类型菜单、碰撞避让和结果排列均改为使用实际尺寸。
- 文本、图片和视频节点增加四角缩放手柄；文本可自由缩放并限制最小 180×120px，媒体节点保持比例、短边最小 96px，所有节点最长边限制为 1200px。
- 图片加载原始尺寸、视频加载元数据后在当前范围内等比收紧逻辑边框，节点位置同步居中修正，使可见媒体、加号和连线边缘保持一致。
- 节点宽高随现有图数据写入本地存储；旧数据缺少宽高时继续使用 272×184px，图数据版本保持为 1。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 38 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 图数据与几何测试覆盖旧数据兼容、宽高持久化、非法成对尺寸回退、四角固定对角、文本最小尺寸、媒体比例与边界、素材等比适配、实际尺寸连线/拖线及相邻节点布局。
- 本地浏览器验收：64×64 图片节点从默认逻辑框收紧为 184×184px，图片、缩放层和左右加号层边界一致；图片来源连线从其右侧真实边缘起始，刷新后尺寸和连线仍保持。四角手柄默认透明，指针进入手柄区域后可见。当前浏览器控制层没有真实拖拽接口，拖拽距离、固定对角和画布缩放换算由上述自动化几何测试验证。

### Notes
- `app/canvas/graph.ts`：增加节点宽高、尺寸解析、文本/媒体缩放约束、媒体自适应及统一实际尺寸几何计算。
- `app/page.tsx`：接入媒体原始尺寸加载、四角指针缩放状态与实际尺寸节点/加号/菜单渲染。
- `app/globals.css`：增加四角手柄、交互边界提示和缩放光标样式。
- `tests/graph.test.mjs`：增加尺寸持久化、缩放约束、媒体自适应和实际尺寸连线测试。
- `tests/rendered-html.test.mjs`：增加媒体加载事件、四角手柄、实际宽高渲染和样式回归断言。
- `docs/canvas.md`：记录四角缩放、媒体比例、尺寸边界、自适应连线和本地恢复规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“增加图片与视频分辨率选择”记录对应的工作树状态。

## 2026-08-06 - Task: 优化节点无框选中与角落缩放

### What was done
- 移除普通节点和无框图片节点的蓝色选中描边，保留节点选择、底部输入框锁定和绿色连线目标反馈。
- 将缩放反馈从整张卡片悬停中拆出，取消缩放层蓝色边界和四角同时显示；每个角改为 24px 透明命中区，仅当前悬停、聚焦或拖动的角显示 8px 圆点。
- 保留四个方向的缩放光标与原有指针捕获，并将右上角命中区向外调整，避免遮挡图片删除按钮。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 38 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 页面回归断言覆盖无选中蓝框、无整层缩放边界、24px 命中区、8px 单角圆点、右上角外移及删除按钮层级。
- 本地浏览器验收：节点未选中和选中后均无蓝框；四角默认全部隐藏，进入左上角时仅左上圆点显示；图片右上缩放区与删除按钮边界不重叠。验收临时创建的空文本节点已删除，未提交生成请求。

### Notes
- `app/page.tsx`：移除选中视觉属性和整卡驱动的缩放展示状态，保留原有缩放数据流。
- `app/globals.css`：移除蓝色选中/缩放边框，增加扩大后的透明角落命中区和单角圆点反馈。
- `tests/rendered-html.test.mjs`：更新节点选中和角落缩放样式回归断言。
- `docs/canvas.md`：记录无框选中、单角显示和 24px 命中规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为提交 `1b3a016`（`feat: add LingkeAI canvas node workflow`）。

## 2026-08-07 - Task: 固定节点连线为右出左进

### What was done
- 将所有正式连线固定为来源节点右侧中心输出、目标节点左侧中心输入，节点移动、缩放或跨越后只更新坐标，不再自动换到上、下或另一侧。
- 保持整张目标卡片可接收拖线；右侧拖线吸附为当前到目标，左侧拖线吸附为上游到当前，现有边结构和持久化版本不变。
- 将截图中的 Vite 错误按开发服务中断处理，干净重启 3001 服务后恢复稳定 HTTP 响应，未向画布业务代码加入规避逻辑。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 38 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 几何测试覆盖目标位于来源右、左、上、下以及节点使用自定义尺寸时，端点始终固定为来源右侧和目标左侧；页面回归覆盖右侧拖线与左侧上游拖线的吸附方向。
- 开发服务重启后 `HEAD /` 与 `GET /` 均返回 200，服务端页面包含画布、连线层和 AI 输入组件，终端未再出现 HMR 发送错误。内置浏览器因本地地址安全策略拒绝继续自动化操作，本轮未完成可视化拖线验收。

### Notes
- `app/canvas/graph.ts`：将正式连线路径改为固定右侧输出、左侧输入及水平切线。
- `tests/graph.test.mjs`：更新不同相对方向、实际尺寸和自由拖线端点测试。
- `tests/rendered-html.test.mjs`：增加左右拖线吸附方向的页面集成断言。
- `docs/canvas.md`：记录固定端口、整卡松开和左右加号方向规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为本轮开始前包含“无框选中与角落缩放”改动的工作树状态。

## 2026-08-07 - Task: 增加触控板平移、框选与整组移动

### What was done
- 将普通鼠标滚轮和触控板双指滑动改为画布横纵平移，仅保留 `Ctrl + 滚轮`与触控板捏合缩放，并将捏合灵敏度提升为原实现的 1.25 倍。
- 将画布空白拖动改为框选：超过 6px 后显示框选区域，按节点实际宽高执行相交选择，空白单击清除选择。
- 为选中节点增加灰色虚线总框；拖动已选节点或总框空白区域会整体移动全部已选节点，现有连线随位置实时更新。
- 保持节点按钮、四角缩放、左右加号、详情和连线交互优先；多节点框选不会锁定底部单节点编辑器，选择状态不写入本地持久化。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 40 项测试通过。
- `git diff --check`：通过，无空白符错误。
- 几何测试覆盖实际尺寸相交选择、不相交排除、选中联合边界、选中节点等距移动、未选节点不移动及连线数据保持；视口测试确认捏合系数从 0.01 调整为 0.0125。
- 本地浏览器验收确认普通双指滚动将画布背景从 `0px 0px` 平移至 `-80px -120px`，空白拖拽释放后框选层正常收起且控制台无运行错误。验收环境没有已有节点，为避免真实生成费用，三类节点混合框选与整组拖动由几何和页面回归测试覆盖，未发起模型请求。

### Notes
- `app/canvas/viewport.ts`：将触控板捏合缩放灵敏度提高为原实现的 1.25 倍。
- `app/canvas/graph.ts`：增加节点实际边界、框选相交、选中联合边界和多节点等距移动操作。
- `app/page.tsx`：接入滚轮平移、空白框选、临时选择状态、灰色总框及整组拖动事件。
- `app/globals.css`：增加框选区域和灰色虚线总框样式，并将空白画布光标改为框选提示。
- `tests/graph.test.mjs`：增加框选几何、联合边界和整组移动单元测试。
- `tests/viewport.test.mjs`：增加 1.25 倍捏合灵敏度精确断言。
- `tests/rendered-html.test.mjs`：增加平移/捏合分流、框选层、总框和批量移动页面回归断言。
- `docs/canvas.md`：更新触控板手势、框选规则、整组移动和临时选择状态说明。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“固定节点连线为右出左进”记录对应的工作树状态。

## 2026-08-07 - Task: 移除单节点选框并透传节点画布手势

### What was done
- 将灰色虚线选中总框限制为两个及以上有效节点；单节点继续保留逻辑选择和底部编辑器联动，但不再显示额外边框。
- 将画布滚轮与触控板监听扩展到节点主体、删除按钮、左右加号和四角缩放区域，使鼠标位于节点上时仍可平移或捏合缩放画布。
- 保留左键节点拖动、多节点总框拖动和框选过程反馈；底部输入组件、节点详情层和类型菜单继续隔离画布手势。

### Testing
- `npm run lint`：通过，无 ESLint 错误或警告。
- `npm test`：通过，Vinext 构建成功，全部 40 项测试通过。
- 页面回归断言覆盖多选总框门槛、节点 `data-node-id` 手势透传范围，以及原有平移、捏合和整组移动逻辑。
- 本地浏览器验收确认页面正常加载，输入组件上的滚轮不会改变画布位置，控制台无运行错误；未发起模型生成请求。
- `git diff --check`：通过，无空白符错误。

### Notes
- `app/page.tsx`：增加两个有效节点的总框显示门槛，并将节点交互层纳入画布滚轮手势范围。
- `tests/rendered-html.test.mjs`：更新单选无框和节点手势透传的页面回归断言。
- `docs/canvas.md`：记录单节点无框、多选有框及节点上画布手势规则。
- `progress.md`：追加本轮实施、验证、文件清单和回滚信息。
- 回滚方式：将本轮改动形成独立提交后执行 `git revert <本轮提交哈希>`；提交前的业务回滚点为上一轮“增加触控板平移、框选与整组移动”记录对应的工作树状态。
