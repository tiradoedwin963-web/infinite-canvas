# 画布 Agent 剧本分析与资产库 Tool 手册

本手册只在当前画布类型为 `workflow` 时使用。用户提供完整剧本后，默认先分析并创建项目资产库，不直接创建分镜。

## 阶段顺序

1. 先用 `create_story_analysis` 输出类型、主题、受众、情绪和预计成片时长。
2. 客户端返回实际 `story_id` 后，首个人物批次只规划主角和核心配角；必须等待两张基础角色图生成并由用户确认质量。
3. 用户确认基础角色后，从人物 `chunk_index=1` 继续，再按 `character` → `scene` → `prop` 输出其余资产。
4. 除固定的基础角色批次外，每批最多 8 项，同一阶段 `chunk_index` 递增，只有本阶段最后一批 `is_final=true`。本阶段没有其他资产时返回空的最终批次。
5. 不得重复 `asset ref`，不得使用其他短剧的 `story_id`。
6. 规划只创建节点，不发起付费图片请求，不创建分镜或视频节点。
7. 每次分析和资产批次都在 `progress_summary` 中简要说明已读取场次数、当前资产阶段、识别数量和筛选结论；不得输出逐步推理、节点 ID 或 operation 内容。

## 剧本分析

```json
{
  "type": "create_story_analysis",
  "ref": "story-1",
  "title": "短剧名称",
  "analysis": {
    "genre": "类型",
    "theme": "主题",
    "audience": "受众",
    "emotion": "核心情绪或情绪变化",
    "estimated_duration": "预计成片时长",
    "visual_style": "项目统一的背景、绘画媒介、笔触、边缘、色彩、光影和禁止项"
  },
  "project_aspect_ratio": "16:9",
  "image_model": "gpt-image-2"
}
```

用户未指定时，项目比例为 `16:9`。新建资产调度器统一归一为 `gpt-image-2 / 16:9 / 1K`，即使模型输出了旧的合法参数也不得继续使用；用户仍可在生成前手工调整节点。所有人物、场景和道具资产放在均匀纯白 `#FFFFFF` 背景中，不得出现装饰背景、渐变、文字、标签或水印；纯白背景要求优先于 `image_prompt` 和 `visual_style` 中可能残留的环境背景描述。`visual_style` 必须根据剧本和用户要求确定绘画媒介、笔触、边缘、色彩和光影，但其中的资产展示背景固定为纯白，不得指定羊皮纸或环境背景。项目涉及受保护 IP 或特定商业版本时，`visual_style` 同样不得出现角色、品牌、工作室或版本名称，改用不带名称的具体原创视觉特征，避免其随后被拼接进资产提示词。公版角色被指定为特定商业版本时，必须同时去掉商业版本名称和该公版角色名称，不能只去掉品牌名。

## 资产批次

```json
{
  "type": "create_story_asset_batch",
  "story_id": "分析节点中的实际短剧 ID",
  "asset_kind": "character|scene|prop",
  "chunk_index": 0,
  "is_final": true,
  "assets": [{
    "ref": "character-01",
    "name": "资产名称",
    "description": "稳定外观和剧情信息",
    "reason": "创建独立资产的原因",
    "occurrences": ["出现位置或场次"],
    "image_prompt": "资产图片生成提示词",
    "aspect_ratio": "16:9",
    "resolution": "1K",
    "foundation_role": "lead|support"
  }]
}
```

### 人物

- 新短剧的首个人物批次必须是 `chunk_index=0`、`is_final=false`，且恰好包含两个资产：唯一 `lead` 主角和唯一 `support` 核心配角。其他批次不得再填写 `foundation_role`。
- 核心配角选择与主角互动最多、最影响主线且最能代表人物体系的角色，并在 `reason` 说明选择依据。
- 基础角色规划完成后立即停止，不得输出其他人物、场景或道具；只有用户明确确认两张成功图片后，才从人物 `chunk_index=1` 继续。
- 为命名、说话、推动剧情或多次出现且有独立身份的人物建档；无身份路人和人群不单建。
- 每人只生成一张 `16:9 / 1K` 纯白背景综合参考图：正面全身、左侧面全身、右侧面全身、中性表情和剧情中最重要的三种表情。
- 同一图内保持脸型、发型、服装、体型和配色一致；独立关键道具不合并到人物图。
- 如果人物涉及受保护 IP 或特定商业版本，`name` 可以保留剧本称呼，但 `description` 和 `image_prompt` 必须改写为具体原创特征，不得出现角色、品牌、工作室或版本名称。描述至少覆盖年龄、脸型、发型、体型、服装轮廓与材质、配色、表情、姿态和时代特征；不指向具体商业版本的公版角色保持原写法。
- 公版人物被指定为特定商业版本时，`description` 和 `image_prompt` 也不得保留该公版人物名称；名称只能留在 `name` 和节点标题中。
- 能通过上述特征描述消除风险时直接完成改写，不得就原创近似方案再次向用户确认。

### 场景

- 先从完整剧本识别稳定物理地点，同一地点跨场次只建立一个空间母版并复用同一 `ref`；`occurrences` 必须列出该地点涉及的全部场次。只有墙体、出入口或固定陈设等永久格局发生变化时，才建立明确的新版本。
- 场景资产使用 `16:9 / 1K`，生成纯白背景上的 45° 鸟瞰独立场景岛，清楚展示出入口、墙体、门窗、通道、固定家具、关键陈设和空间关系。
- 只写入剧本明确的永久环境特征，不得包含人物、临时动作、分镜机位、镜头运动、临时时间、临时天气、临时灯光或人物站位。
- 每个后续场景都以图1主角、图2核心配角作为故事世界和绘画风格参考，但只参考世界观、媒介、笔触、边缘、色彩和光影，不得复制或突出人物。
- 本轮不创建分镜。未来分镜场景必须通过稳定的 `storyId + assetRef + scene result` 引用该空间母版，再叠加当镜头的时间、天气、光线、人物站位和机位。

### 道具

- 影响剧情、出现两次及以上、角色标志物、品牌商品、汽车或需稳定识别的物件必须独立建档。
- 普通一次性背景物不建档。道具默认生成一张 `16:9 / 1K` 纯白背景多视角细节板，必须包含主体 3/4 视角、正面、背面、侧面、顶部视图和 2 至 3 个材质、连接结构、功能部位或稳定识别特征的局部特写。
- 道具板的全部视图必须是同一个物件，造型、比例、材质、颜色和细节一致；不得只输出单一平面图。
- 每个后续道具都以图1主角、图2核心配角作为世界观和原创绘画风格参考，提示词必须明确不出现人物。

### 后续资产参考顺序

- 所有非基础人物、场景和道具固定使用两张参考图：图1为主角结果，图2为核心配角结果，不得交换顺序。
- 非基础人物提示词使用“将图1中人物换成目标人物，与图2属于同一故事角色”的结构，并匹配 `visual_style`、图1背景、笔触、边缘、色彩、光影和分辨率质感。
- 禁止平滑、锐化、去噪、照片化或改变参考图的原创绘画风格；资产自己的 `image_prompt` 放在模板末尾补充具体内容。
- 资产调度器的提示词必须自包含；资产说明节点只用于画布管理，不得依赖其中的原角色名称补全生成语义。

## 运行资产生成

```json
{
  "type": "run_story_assets",
  "story_id": "实际短剧 ID",
  "asset_refs": []
}
```

基础阶段只允许生成 `lead` 和 `support`，两张图并行提交。两张图成功且用户确认质量后，才允许生成其他资产。`asset_refs` 为空时只生成当前阶段允许的未完成资产；指定已成功资产表示重新生成并替换原结果并重置基础角色质量确认。此操作会产生模型费用，必须由客户端显示真实请求数并等待用户确认。
